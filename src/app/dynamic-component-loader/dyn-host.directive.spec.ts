import { DynHostDirective } from './dyn-host.directive';

describe('DynHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DynHostDirective();
    expect(directive).toBeTruthy();
  });
});
