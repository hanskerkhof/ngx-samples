/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RouteDataDetailComponent } from './route-data-detail.component';

describe('RouteDataDetailComponent', () => {
  let component: RouteDataDetailComponent;
  let fixture: ComponentFixture<RouteDataDetailComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RouteDataDetailComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RouteDataDetailComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
